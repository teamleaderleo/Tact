# Decision from the frozen September 20 receipts

The consolidation coincides with much less executed work, but these windows cannot establish net savings for equivalent accepted changes. The report's most useful next reduction is the existing shallow-complexity-checkout PR, not another cache backend switch.

The fixed, all-workflow windows contain 668 before / 474 after runs and 672 / 476 attempts. Observed runner time falls from 7,126.12 to 1,085.70 minutes. These are run-created cohorts: execution can finish later. Cancelled jobs with no step timestamps (65 before, 35 after) have unknown consumption and are excluded, so both totals are lower bounds. Failed and cancelled attempts with timings remain included.

There are zero merged PRs in the before hour and two in the after hour. No per-accepted-change before/after reduction is identifiable. The after throughput ratio is 542.85 runner-minutes per merged PR, with work on unmerged changes in its numerator. The number of primary CI attempts reaching app-host unit tests falls from 28 to zero; merge-validation consumption falls from 564.72 minutes to zero. Thus a large part of the aggregate difference is removed/deferred coverage and a changed event mix, not evidence that equivalent full validation became faster.

The paired Release build step really is 1,740 → 234 seconds in the GitHub job metadata. The first-step-to-completion job interval is 1,991 → 447 seconds: 1,544 / 1,991 = 77.55% less time for this pair. The [#13168 description](https://github.com/manaflow-ai/cmux/pull/13168) claims unchanged app inputs; the two source revisions and jobs are pinned in the generated report. Its 1,992 → 448 job figures include a slightly different timestamp boundary. This reconciles the quoted 77.5% without extending it to changed sources or fleet cost. The separate Debug and Release seed jobs consume 2,701 seconds (45.02 minutes), plus four seconds of decision overhead. Seeds and saves cannot be ignored or counted as a second independent saving. This pair's build logs were unavailable, so backend/archive details are attributed to the PR, not “observed” from green cache steps.

## Current execution versus historical rollout

The current snapshot reads `compile-only`, `r2`, and Warp macOS 15 routing. Every after-window CI head inspected has an R2-capable cache action; 48 before-window CI heads have no such action at a readable commit, while the experimental R2 PR itself accounts for one pre-merge capable head. A before/after wall-clock boundary does not cleanly separate capabilities.

A retained [failed consumer job](https://github.com/manaflow-ai/cmux/actions/runs/35509887189/job/106076170670), outside both cohorts, records an ordinary cache-action miss and **R2 prefix** restores for packages and compilation state. `fixtures/observed-r2.json` retains allowlisted timestamped lines, source head, parser output and the original raw-log hash. It proves some real consumption after rollout; it does not prove a population hit rate or that all compiler failures are cache misses. The provider job outcome stays failed.

The [09:30 runbook](https://github.com/manaflow-ai/cmux/issues/13182) saying main lacked R2 predates #13168's 10:18 merge. The last inspected ruleset 15917555 has no merge-queue rule; this is a statement about that ruleset, not an exhaustive policy audit. No after-window merge groups ran. #13176's merged identical-product reuse cannot be credited with observed queue hits from this cohort. #13165 merged during the after hour; #13123 merged at 15:33, after it. Those are confounders, not benefits fully present at 13:00.

Replacement history matters: #13175 merged into #13122's branch; #13122 later landed on main. #13199 closed unmerged and explicitly points to replacement #13204. #13235 is the open watcher correction; the original #13117 merge did not prove fail-fast worked. The generated rollout table pins merge timestamps and revisions; open PR test-merge SHAs are not called landed revisions.

## Three residual problems, ranked by evidence and readiness

1. **Complexity checkout is the clearest current compute reduction.** In the after cohort, the two complexity workflows consume 211.58 runner-minutes, of which **199.57 minutes are checkout steps** (excluding post-job cleanup). Example: [successful 306-second checkout](https://github.com/manaflow-ai/cmux/actions/runs/35512932055/job/106083928875). [#13171](https://github.com/manaflow-ai/cmux/pull/13171) already owns depth-one checkout plus exact comparison-commit fetches and is still open. Next action: have the existing owner complete its trusted-policy review/landing process, then compare the same checkout/check breakdown for a fixed normal-traffic cohort. Some checkout is necessary; 199.57 minutes is exposure, not guaranteed savings. Do not skip a required failure on metadata edits.

2. **Compiled-product delivery has an unresolved long tail.** The before cohort's 168 artifact download steps consume **292.77 minutes**, with p95 **621.15 seconds**. One [successful consumer took 1,082 seconds](https://github.com/manaflow-ai/cmux/actions/runs/35496436415/job/106043131506). Only one artifact-download observation exists after, from a different workflow; zero after app-host consumers means this cohort cannot claim the tail improved. The broader [14:46 audit](https://github.com/manaflow-ai/cmux/issues/13095#issuecomment-5750614492) also found no tail improvement (619 → 654 seconds); its population is not mixed into these totals. Next action: finish existing [#13172](https://github.com/manaflow-ai/cmux/pull/13172) timing/size/outcome receipts, then evaluate [#13201](https://github.com/manaflow-ai/cmux/pull/13201) packaging against complete producer + consumer cost. Do not label step time as network time or assume LZFSE fixes the stall. Both PRs remain open in this snapshot.

3. **Cancellation and merge-validation lifecycle still lack attribution and proof.** The after cohort includes **198.68 runner-minutes inside cancelled attempts**, and **23.35 retry minutes**, overlapping the main total and each other. They are consumed work, not automatically waste. Historical [merge group 35497814823](https://github.com/manaflow-ai/cmux/actions/runs/35497814823) has **1,316 aggregate runner-seconds after the first failed job** in this cohort. Queue-removal identity and cancellation causes remain unknown. Next action: finish existing [#13235](https://github.com/manaflow-ai/cmux/pull/13235), then an authorized operator must establish watcher attachment and cancellation on a controlled merge group before relying on it. Any canary or settings change is outside this read-only assignment. Continue observing cancelled PR admission and fingerprint/reuse verdicts; do not count hypothetical remaining cancelled work as saved time.

This ranking favors an existing concrete reduction, then a measured bottleneck needing diagnosis, then a validation/control gap. It is not a sum of avoidable costs. The existing [web-consolidation PR #13170](https://github.com/manaflow-ai/cmux/pull/13170) is another owned opportunity; the broad audit's duplicated-step estimate is not transplanted to these windows.

## Evidence needed to close the remaining questions

- Readable build/consumer logs for the exact sampled job endpoints marked `request_failed` or `timeout_or_invalid_response`; 16 of 19 sampled logs failed. The three readable logs are decision jobs, not cache consumers. No hit-rate inference is possible.
- Archive/pointer identities, matched key, producer run/attempt/source, seed age, bytes and monotonic restore/save timings; provider-side storage/requests and observed contract prices before estimating money.
- Actual checkout and workflow-execution SHA where checkout ref overrides apply. Run `head_sha` and inspected source are separate observations; most fields remain explicitly unknown.
- Test selection/count and failure-classifier receipts, plus artifact contents, to distinguish valid skips, zero tests, known baseline failures and new failures. This collector records executed steps and outcomes but does not promote a green step to coverage proof.
- Queue-entry/exit and administrative audit evidence to identify discarded work. All merge-group outcomes are retained; cancelled is not equated with discarded.
- A representative matched change/architecture/suite cohort with seed amortization and billed runner rounding. Current rates, model spend and storage billing are absent; dollars are intentionally null.

The complete report replays offline with one command. There is no need to dispatch, rerun, cancel, change settings, provision access or modify production to reproduce these findings.
